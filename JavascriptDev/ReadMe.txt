After importing this as a Maven project, start the project in Eclipse:
1) right click the DemoApplication.java
2) click Run as Java Application (Spring Boot comes with Tomcat built-in, so no need to Run on Server)